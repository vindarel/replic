image: daewok/lisp-devel

# stages:
  # - test
  # - build

test:
  stage: test
  only:
    - tags
  script:
    - make test

build:
  stage: build
  only:
    # Only when a new tag is pushed.
    - tags
  script:
    - make build
  artifacts:
    paths:
      # Include the version number ?
      - replic
